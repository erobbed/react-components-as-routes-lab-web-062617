import React from 'react';
import { render } from 'react-dom';
import App from './containers/App';
import Browser as Router from 'react-router'

render (
  <App />,
  document.getElementById('root')
);
